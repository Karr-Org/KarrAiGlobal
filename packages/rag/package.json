{
    "name": "@makemyai/rag",
    "version": "0.1.0",
    "private": true,
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "scripts": {
        "type-check": "tsc --noEmit",
        "clean": "rimraf dist"
    },
    "dependencies": {
        "@makemyai/ai": "workspace:*",
        "@makemyai/database": "workspace:*"
    },
    "devDependencies": {
        "typescript": "^5.3.0",
        "rimraf": "^5.0.5"
    }
}